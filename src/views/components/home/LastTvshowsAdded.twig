<section id="last-tvshows-added" class="last-tvshows-added max">
    <h2 class="section-title">Ultimas Series Añadidas</h2>
    <ul class="tvshows-grid">
        {% for tvShow in lastTvShowsAdded %}
            <li>
                {%
                    include 'components/MovieCard.twig' 
                    with { 
                        title: tvShow.title, 
                        image: tvShow.poster, 
                        slug: "/serie/" ~ tvShow.slug
                    } only
                %}
            </li>
        {% endfor %}
    </ul>
</section>
<style>
    #last-tvshows-added {
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
        margin: 0 auto;
        width: 100%;
        .tvshows-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            list-style: none;
            padding: 0;
        }
    }
</style>